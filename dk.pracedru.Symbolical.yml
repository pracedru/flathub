id: dk.pracedru.Symbolical
runtime: org.kde.Platform
runtime-version: '6.10'
sdk: org.kde.Sdk
base: com.riverbankcomputing.PyQt.BaseApp
base-version: '6.10'
command: symbolical
finish-args:
  - --share=ipc
  - --socket=fallback-x11
  - --socket=wayland
  - --device=dri
  - --socket=pulseaudio
  - --env=QT_AUDIO_BACKEND=pulseaudio
  - --env=QT_SCALE_FACTOR_ROUNDING_POLICY=Round
  - --socket=cups
cleanup-commands:
  - /app/cleanup-BaseApp.sh
modules:
  - name: symbolical
    buildsystem: meson
    sources:
      - type: git
        url: https://gitlab.com/symbolical/symbolical.git
        commit: 1fb8b7e655a202ccb603c533efc3b063e00cb655
        tag: v0.5.0.5
